schema: spec-driven

context: |
  Tech stack: Kotlin, Spring Boot 4, Spring AI, SQLite, Maven
  Testing: Use MockK (not Mockito) with @MockkBean from springmockk for Spring integration tests
  Domain: AI-powered podcast pipeline (content monitoring, LLM summarization, TTS, RSS feed)

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
